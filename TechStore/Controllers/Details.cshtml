@page
@model TechStore.Controllers.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>CountryOrder</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.CountryOrder.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.CountryOrder.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.CountryOrder?.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
